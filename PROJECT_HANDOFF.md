# ملف التسليم الشامل — Crochet by Asma

> وثيقة مرجعية للمساعد التقني الذي سيراجع المشروع ويكمل تطويره.
> آخر تحديث: 2026-08-01 · الفرع الرئيسي: `main`

---

## 1. نظرة عامة على المشروع

| البند | التفصيل |
|---|---|
| **اسم المشروع** | كروشيه أسماء — Crochet by Asma |
| **نوع الموقع** | متجر إلكتروني + معرض أعمال لمنتجات يدوية (كروشيه، مطبوعات، طباعة ثلاثية الأبعاد) |
| **اللغة والاتجاه** | عربي بالكامل، RTL، Mobile First |
| **الهدف** | بيع قطع يدوية جاهزة، واستقبال طلبات مخصصة (هدايا/توزيعات باسم ومناسبة)، مع لوحة تحكم لإدارة المتجر |
| **الحالة** | **مشروع Frontend كامل وقابل للتجربة** — وليس مجرد نموذج شكلي. كل التدفقات تعمل فعليًا كمحاكاة (front-end simulation) |
| **Backend** | ❌ **لا يوجد Backend حقيقي.** لا API، لا قاعدة بيانات، لا خادم. البيانات كلها في `localStorage` داخل متصفح الزائرة |

### ماذا يعني "قابل للتجربة"؟
كل الأزرار تعمل، والطلبات تُنشأ برقم حقيقي، والسلة تحسب الإجمالي والضريبة والشحن، ولوحة التحكم تعدّل بيانات فعلية تُحفظ. لكن كل هذا **محلي في متصفح واحد** — لا يصل شيء لأسماء ولا لأي خادم.

### الرابط المنشور
- **الإنتاج:** https://crochetbyasma.com · https://www.crochetbyasma.com
- **Vercel:** https://crochetbyasma-crochetbyasma-team.vercel.app
- **المستودع:** https://github.com/crochetbyasma/crochetbyasma

### بيانات تجريبية للتجربة السريعة
| الوظيفة | القيمة |
|---|---|
| تتبع طلب | `CBA-1042` أو `CBA-1043` |
| دخول الحساب | أي اسم + جوال `0501234567` |
| كود خصم | `SHUKRAN10` (خصم 10%) |
| لوحة التحكم | زر "لوحة التحكم" في القائمة العلوية — كلمة المرور: `admin123` |

---

## 2. التقنية المستخدمة

| البند | التفصيل |
|---|---|
| **الإطار** | **Vite 5** + **React 18** — ليس Next.js |
| **اللغة** | **JavaScript (JSX)** — لا يوجد TypeScript |
| **التصميم** | **CSS خام مكتوب يدويًا** داخل `src/App.jsx` كنص في متغير `CSS` يُحقن عبر `<style>` — لا Tailwind، لا CSS Modules، لا styled-components |
| **الحركة/الأنيميشن** | **CSS keyframes فقط** — لا Framer Motion ولا أي مكتبة حركة |
| **الرسومات والشعار** | **SVG مكتوب داخل الكود** (inline) — لا ملفات صور خارجية |
| **الخطوط** | Google Fonts عبر `@import`: `Alexandria` (النصوص) و `Playpen Sans Arabic` (خط اليد) |
| **الحزم الخارجية** | **صفر** — التبعيات هي `react` و `react-dom` فقط |
| **إدارة الحالة** | `useState` في مكوّن `App` وتمرير Props — لا Redux ولا Context |
| **التوجيه (Routing)** | ❌ **لا يوجد router.** التنقل عبر حالة `route` في React. راجع "المشاكل المعروفة" |

### تشغيل المشروع محليًا
```bash
npm install
npm run dev          # يفتح على http://localhost:5173
```

### بناء المشروع للنشر
```bash
npm run build        # يُخرج مجلد dist/
npm run preview      # معاينة نسخة الإنتاج محليًا على http://localhost:4173
```

**بيئة التطوير المستخدمة:** Node v22 · npm v10 (يعمل على Node 18+)

**حجم البناء الحالي:** ملف JS واحد ~413 KB (‏178 KB مضغوط gzip) — راجع "تحتاج تطوير لاحقًا".

### إعدادات Vercel الصحيحة

| الإعداد | القيمة |
|---|---|
| **Framework Preset** | `Vite` |
| **Root Directory** | `./` (جذر المستودع) |
| **Build Command** | `npm run build` |
| **Output Directory** | `dist` |
| **Install Command** | `npm install` (افتراضي) |
| **Node Version** | `24.x` (مضبوط حاليًا في Vercel — يعمل على 18+) |
| **Environment Variables** | ❌ **لا يوجد أي متغيرات بيئة** |

**ملف `vercel.json`** يحتوي على rewrite واحد يوجّه كل المسارات إلى `/` (نمط SPA):
```json
{ "rewrites": [{ "source": "/(.*)", "destination": "/" }] }
```

**النشر تلقائي:** أي دمج في فرع `main` ينشر تلقائيًا على الإنتاج.

---

## 3. هيكل الملفات

```
crochetbyasma/
├── index.html              ← نقطة الدخول: meta tags، عنوان الصفحة، dir="rtl"، lang="ar"
├── package.json            ← التبعيات والأوامر
├── vite.config.js          ← إعداد Vite (React plugin فقط)
├── vercel.json             ← rewrite لنمط SPA
├── README.md               ← تعليمات تشغيل مختصرة
├── PROJECT_HANDOFF.md      ← هذا الملف
├── public/
│   └── favicon.svg         ← أيقونة المتصفح (الملف الخارجي الوحيد)
└── src/
    ├── main.jsx            ← تهيئة React وربطه بـ #root (10 أسطر)
    ├── index.css           ← إعادة تعيين CSS بسيطة جدًا (3 أسطر)
    └── App.jsx             ← ⚠️ المشروع كله: ~2976 سطر
```

### شرح المجلدات

| المجلد/الملف | الوظيفة |
|---|---|
| `public/` | ملفات ثابتة تُنسخ كما هي إلى `dist/` — يحتوي على `favicon.svg` فقط |
| `src/main.jsx` | نقطة انطلاق React — يستورد `App` و `index.css` |
| `src/index.css` | reset بسيط جدًا (هوامش وخلفية) — **معظم التنسيق ليس هنا** |
| `src/App.jsx` | **كل شيء**: التنسيق، البيانات، المكونات، الصفحات، لوحة التحكم |

### ⚠️ ملاحظة مهمة جدًا عن الهيكل
**لا توجد مجلدات `components/` أو `pages/` أو `data/` أو `styles/` أو `assets/`.**
المشروع كله ملف واحد `src/App.jsx` بطول ~2976 سطر، منظّم داخليًا بتعليقات فاصلة:

```
سطر   14  ← const CSS       : كل تنسيقات الموقع (~380 سطر CSS)
سطر  426  ← ثوابت وبيانات   : OCCASIONS، ORDER_STATUSES، initial*
سطر  612  ← أدوات مساعدة    : SAR، uid، today، calcTotals
سطر  633  ← مكونات صغيرة    : Ph، Field، Qty، Stars، Modal، Upload
سطر  683  ← عناصر فنية      : Art، DoodleField، Reveal، Magnetic، KineticTitle
سطر  945  ← طبقة التخزين    : DB_KEY، dbLoad، dbSave، dbClear، ImgUploader
سطر 1005  ← صفحات المتجر    : ProductCard، Home، Shop، ProductDetail، Gallery...
سطر 1587  ← معالج الطلب     : Wizard
سطر 1806  ← السلة والدفع    : CartPage
سطر 1954  ← تتبع/حساب       : Track، Account
سطر 2100  ← لوحة التحكم     : Admin وكل تبويباتها
سطر 2740  ← المكوّن الرئيسي : App (الحالة، الهيدر، التوجيه، الفوتر)
```
**هذا أهم بند يحتاج إعادة هيكلة** — راجع القسم 10.

---

## 4. صفحات العميلة

التنقل عبر حالة داخلية `route.page` — **لا يوجد مسار URL لكل صفحة** (كلها على `/`).

| # | الصفحة | مفتاح `route.page` | الوظيفة |
|---|---|---|---|
| 1 | **الرئيسية** | `home` | واجهة رئيسية بأقسام: hero بعنوان وأزرار وبطاقة القطعة المميزة · شريط متحرك · التصنيفات · 4 منتجات مختارة · قسم الطلب الخاص · "كيف نعمل؟" بثلاث خطوات · معرض مصغّر · آراء العميلات · ختام |
| 2 | **المتجر** | `shop` | شبكة كل المنتجات مع بحث نصي وفلترة بالتصنيف والنوع (جاهز/مخصص) وترتيب. أزرار المفضلة تعمل هنا |
| 3 | **صفحة المنتج** | `product` (+`id`) | صور المنتج، السعر، الوصف، حقول التخصيص (اسم/عبارة/مناسبة/تغليف/تاريخ/استعجال/ملاحظات)، عدّاد كمية، إضافة للسلة، وقسم تقييمات مع إمكانية إضافة تقييم |
| 4 | **الطلب المخصص** | `wizard` | معالج من 4 خطوات بمسارين (منتج جاهز + تخصيص / مخصص بالكامل)، مع ملخص جانبي متجدد وشاشة نجاح برقم طلب |
| 5 | **السلة** | `cart` | عرض المنتجات مع تخصيصاتها، تعديل الكمية، حذف، كود خصم، وملخص (فرعي/خصم/شحن/ضريبة/إجمالي) |
| 6 | **الدفع** | `cart` (نفس الصفحة) | ليست صفحة منفصلة — تظهر داخل السلة عند الضغط على "متابعة إتمام الطلب": نموذج بيانات العميلة + طريقة الدفع + تأكيد → شاشة تأكيد برقم الطلب |
| 7 | **تتبع الطلب** | `track` | إدخال رقم طلب (مثل `CBA-1042`) لعرض الحالة وخط زمني للمراحل وملاحظات المتجر |
| 8 | **حساب العميلة** | `account` | دخول وهمي بالاسم والجوال، ثم عرض الطلبات المرتبطة بالرقم + المفضلة + العناوين (إضافة/حذف) + نقاط ولاء (قريبًا) |
| 9 | **معرض الأعمال** | `gallery` | شبكة أعمال سابقة مع فلترة بالتصنيف والمناسبة، وزر "أريد عملًا مشابهًا" يفتح المعالج |
| 10 | **عن المتجر والسياسات** | `about` | نبذة + 3 بطاقات مميزات + مدة التنفيذ + سياسة الإلغاء والاستبدال |
| 11 | **التواصل** | `contact` | أزرار واتساب وإنستغرام + نموذج رسالة (شكلي فقط) |
| 12 | **لوحة التحكم** | `admin` | راجع القسم 5 |

---

## 5. صفحات لوحة التحكم

الوصول: زر "لوحة التحكم" في القائمة العلوية → **شاشة تسجيل دخول** (كلمة المرور الحالية: `admin123`).
⚠️ الحماية **مؤقتة وواجهية فقط**: كلمة المرور مكتوبة في `src/App.jsx` (الثابت `ADMIN_PASSWORD`) وأي شخص يفتح ملف الجافاسكربت في المتصفح يستطيع قراءتها. تمنع الزائر العادي فقط. حالة الدخول في `sessionStorage` (المفتاح `cba-admin-session`) وتنتهي بإغلاق التبويب.

كل التبويبات **تفاعلية فعليًا** وتعدّل البيانات وتُحفظ في `localStorage`.

| # | الشاشة | الحالة | التفاصيل |
|---|---|---|---|
| 1 | **لوحة المعلومات** | ✅ تفاعلية | إحصائيات محسوبة فعليًا من الطلبات (إجمالي، نشطة، مبيعات، منتجات معروضة) + تنبيه مخزون منخفض + أحدث 5 طلبات |
| 2 | **الطلبات** | ✅ تفاعلية | جدول بكل الطلبات، بحث بالرقم/الاسم/الجوال، فلترة بالحالة، **تغيير الحالة يعمل ويُسجّل في الخط الزمني**، فتح التفاصيل، عرض الفاتورة |
| 3 | **تفاصيل الطلب** | ✅ تفاعلية | لوحة منسدلة داخل تبويب الطلبات: بيانات العميلة، الأصناف، التخصيصات، الملاحظات الداخلية، سجل الحالات |
| 4 | **الطلبات الخاصة** | ✅ تفاعلية | نفس واجهة الطلبات لكن مفلترة على `kind === "custom"` |
| 5 | **المنتجات** | ✅ CRUD كامل | إضافة/تعديل/حذف/إخفاء/تمييز منتج، **رفع صور حقيقي** مع ضغط تلقائي، تعديل السعر والمخزون وخيارات التخصيص |
| 6 | **التصنيفات** | ✅ CRUD كامل | إضافة/تعديل/حذف تصنيف، تعديل الأقسام الفرعية، وإعادة الترتيب بالأسهم ↑↓ |
| 7 | **أكواد الخصم** | ✅ CRUD كامل | إضافة/تعديل/حذف كوبون، نوع (نسبة/مبلغ)، حد أدنى، تفعيل/إيقاف — **والكوبونات تعمل فعليًا في السلة** |
| 8 | **معرض الأعمال** | ✅ CRUD كامل | إضافة/تعديل/حذف عمل مع **رفع صورة حقيقي**، وربطه بتصنيف ومناسبة |
| 9 | **نصوص الموقع** | ✅ تفاعلية | تعديل كل نصوص واجهة الموقع (~60 نصًا) مع حفظ تلقائي ورموز تنسيق (`*تظليل*`، `~خط يد~`، `\|` سطر جديد) وزر استرجاع الافتراضي |
| 10 | **الإشعارات** | ⚠️ عرض فقط | تعرض إشعارات وهمية ثابتة + زر "تحديد الكل كمقروء" يعمل. **لا تُولَّد إشعارات جديدة تلقائيًا عند وصول طلب** |
| 11 | **الإعدادات** | ✅ تفاعلية | اسم المتجر، واتساب، إنستغرام، رسوم الشحن، حد الشحن المجاني، نسبة الضريبة، الأماكن الأسبوعية، طرق الدفع، شركات الشحن، مدة التنفيذ، سياسة الإلغاء + زر مسح كل البيانات. **كلها تنعكس فورًا على الموقع** |
| 12 | **الفاتورة** | ✅ تفاعلية | نافذة فاتورة A4 بشعار المتجر وبيانات العميلة والأصناف والإجماليات + **زر طباعة يعمل** عبر `window.print()` مع تنسيق طباعة مخصص |

---

## 6. المكونات الأساسية

كلها في `src/App.jsx`. الأرقام تقريبية وقد تتغير مع التعديلات.

### مكونات الهيكل العام
| المكوّن | السطر ~ | الوظيفة |
|---|---|---|
| `App` | 2740 | المكوّن الجذر: كل الحالة، التحميل/الحفظ، التوجيه، الهيدر، الفوتر |
| **الهيدر** | داخل `App` ~2895 | شريط إعلان + شعار + أيقونة حساب + أيقونة سلة بعدّاد + قائمة تنقل (☰ منسدلة على الجوال) + إخفاء عند النزول |
| **الفوتر** | داخل `App` ~2950 | شعار + وصف + روابط سريعة + بيانات تواصل قابلة للضغط + سطر سياسة + حقوق |
| `Logo` / `LogoMark` / `LogoScript` | ~965 | الشعار الرسمي كـ SVG inline (نسخة داكنة وفاتحة) |

### مكونات المتجر
| المكوّن | السطر ~ | الوظيفة |
|---|---|---|
| `ProductCard` | 1005 | بطاقة منتج: صورة، تصنيف، اسم، سعر، شارة توفر، زر مفضلة، زر شراء |
| `Home` | 1042 | الصفحة الرئيسية بكل أقسامها |
| `Shop` | 1260 | شبكة المنتجات مع البحث والفلترة |
| `ProductDetail` | 1328 | صفحة المنتج والتخصيص والتقييمات |
| `Gallery` | 1458 | معرض الأعمال |
| `Wizard` | 1587 | معالج الطلب المخصص (4 خطوات + شاشة نجاح) |
| `CartPage` | 1806 | السلة + الدفع + شاشة التأكيد (ثلاث حالات في مكوّن واحد) |
| `Track` | 1954 | تتبع الطلب والخط الزمني |
| `Account` | 2040 | حساب العميلة |
| `AccountAddresses` | ~2005 | إدارة العناوين (إضافة/حذف) |
| `About` / `Contact` | ~1500 | صفحتا السياسات والتواصل |

### مكونات لوحة التحكم
| المكوّن | السطر ~ | الوظيفة |
|---|---|---|
| `AdminLogin` | ~2100 | شاشة قفل لوحة التحكم (كلمة مرور + إظهار/إخفاء + رسالة خطأ) |
| `Admin` | ~2160 | الغلاف: التبويبات الجانبية + زر تسجيل الخروج + التوجيه الداخلي (يقوم بدور DashboardLayout) |
| `AdminDashboard` | ~2150 | الإحصائيات وأحدث الطلبات |
| `AdminOrders` | ~2190 | جدول الطلبات + التفاصيل + تغيير الحالة (يقوم بدور OrderTable) |
| `AdminProducts` | ~2325 | إدارة المنتجات |
| `AdminCategories` | ~2410 | إدارة التصنيفات |
| `AdminCoupons` | ~2455 | إدارة الكوبونات |
| `AdminGallery` | ~2505 | إدارة المعرض |
| `AdminTexts` | ~2650 | محرر نصوص الموقع |
| `AdminSettings` | ~2690 | إعدادات المتجر |
| `InvoiceModal` | 2251 | الفاتورة والطباعة |

### مكونات مشتركة (Forms / Buttons / Modal)
| المكوّن | السطر ~ | الوظيفة |
|---|---|---|
| `Modal` | 661 | نافذة منبثقة عامة (تُستخدم في كل شاشات التحرير بلوحة التحكم) |
| `Field` | 637 | غلاف حقل: تسمية + محتوى + تلميح |
| `Qty` | 645 | عدّاد كمية (− / العدد / +) |
| `Stars` | 653 | تقييم نجوم (عرض وإدخال) |
| `Upload` | 674 | ⚠️ رافع صور **وهمي** للعميلة — يزيد عدّادًا فقط ولا يرفع شيئًا |
| `ImgUploader` | 911 | ✅ رافع صور **حقيقي** للوحة التحكم — يقرأ الملف ويضغطه ويحوّله base64 |
| `Pic` / `Ph` | 904 / 633 | عرض صورة حقيقية أو بديل رمادي عند غيابها |
| **الأزرار** | CSS ~98 | ليست مكوّنًا — أصناف CSS: `.btn`، `.btn.primary`، `.btn.ghost`، `.btn.accent`، `.btn.sm`، `.btn.block`، `.btn.danger` |

### مكونات بصرية وحركية
| المكوّن | السطر ~ | الوظيفة |
|---|---|---|
| `Art` | 970 | مكتبة رسومات SVG يدوية (yarn، heart، gift، flower، star، hook، tag، card) |
| `Reveal` | 763 | ظهور تدريجي عند التمرير عبر `IntersectionObserver` |
| `Magnetic` | 776 | تأثير انجذاب الزر للمؤشر |
| `KineticTitle` | 795 | عنوان بحركة كلمة كلمة |
| `Rich` | 806 | عارض نصوص لوحة التحكم مع رموز التنسيق |
| `HeroMock` | 826 | مشهد بديل داخل بطاقة الهيرو عند غياب صورة المنتج |
| `Confetti` / `SpinBadge` / `DoodleField` / `MarqueeText` | متفرقة | عناصر زخرفية |
| `TrustStrip` / `WhatsFab` / `ThankGift` | 975-992 | شريط ضمانات، زر واتساب عائم، بطاقة شكر |

---

## 7. البيانات الحالية

**كل البيانات الوهمية في ملف واحد: `src/App.jsx`** — كثوابت `initial*` تبدأ من السطر ~426.

| نوع البيانات | اسم الثابت | السطر ~ | المحتوى |
|---|---|---|---|
| **المنتجات** | `initialProducts` | 449 | 10 منتجات (كروشيه/مطبوعات/3D) بسعر ومخزون وخيارات تخصيص — **بدون صور** |
| **التصنيفات** | `initialCategories` | 442 | 4 تصنيفات مع أقسام فرعية |
| **المعرض** | `initialGallery` | 462 | أعمال سابقة بعنوان ومناسبة — **بدون صور** |
| **الكوبونات** | `initialCoupons` | 473 | كوبونات منها `SHUKRAN10` (10%) |
| **الطلبات** | `initialOrders` | 479 | 4 طلبات: `CBA-1040` إلى `CBA-1043` بحالات وسجل مختلف |
| **التقييمات** | `initialReviews` | 508 | 4 تقييمات مرتبطة بمنتجات |
| **الإعدادات** | `initialSettings` | 515 | اسم المتجر، واتساب، إنستغرام، شحن، ضريبة، سياسات، طرق دفع |
| **نصوص الموقع** | `initialTexts` | 531 | ~60 نصًا لكل واجهات الموقع |
| **ترحيل النصوص** | `TEXT_MIGRATIONS` | 592 | يستبدل الصياغات الافتراضية القديمة المحفوظة في متصفحات الزائرات |
| **الإشعارات** | `initialNotifications` | 605 | إشعارات وهمية ثابتة |
| **حالات الطلب** | `ORDER_STATUSES` | 428 | 6 حالات: جديد → قيد التنفيذ → جاهز للشحن → مشحون → مُسلّم (+ ملغى) |
| **المناسبات** | `OCCASIONS` | 426 | زواج، تخرج، مولود، رمضان، عيد... |

### طبقة التخزين
```js
const DB_KEY = "cba-db-v1";        // ~سطر 945
dbLoad()  →  localStorage.getItem
dbSave()  →  localStorage.setItem  (حفظ تلقائي بتأخير 700ms)
dbClear() →  localStorage.removeItem  (زر "مسح البيانات" في الإعدادات)
```

**ما يُحفظ:** `products`, `categories`, `gallery`, `orders`, `coupons`, `reviews`, `notifications`, `settings`, `texts`, `nextNum`

**⚠️ ما لا يُحفظ (يضيع عند تحديث الصفحة):** `cart` (السلة) · `favorites` (المفضلة) · `user` (جلسة الدخول)

**⚠️ الصور تُخزَّن base64 داخل `localStorage`** — الحد الأقصى للمتصفح ~5MB، لذا رفع صور كثيرة قد يملأ المساحة ويوقف الحفظ.

---

## 8. الوظائف التفاعلية — حالة كل وظيفة

| # | الوظيفة | الحالة | التفاصيل |
|---|---|---|---|
| 1 | **إضافة للسلة** | ✅ **Working** | تضيف المنتج مع كل تخصيصاته، مع تأكيد بصري وتحديث عدّاد السلة |
| 2 | **تغيير الكمية** | ✅ **Working** | في صفحة المنتج وفي السلة، والإجمالي يتحدث فورًا |
| 3 | **حذف منتج من السلة** | ✅ **Working** | يحذف السطر ويعيد حساب الإجماليات |
| 4 | **تطبيق كود خصم** | ✅ **Working** | يتحقق من الكود والتفعيل والحد الأدنى، ويطبّق نسبة أو مبلغًا مع رسالة واضحة |
| 5 | **حساب الإجماليات** | ✅ **Working** | فرعي + خصم + شحن (مجاني فوق الحد) + ضريبة % = إجمالي |
| 6 | **إرسال الطلب (Checkout)** | ✅ **Working** | نموذج بيانات + تحقق من الحقول → ينشئ طلبًا برقم `CBA-####` → شاشة تأكيد. **الطلب يظهر فعليًا في لوحة التحكم** |
| 7 | **الدفع التجريبي** | ⚠️ **Partial** | اختيار طريقة الدفع يعمل ويُسجَّل مع الطلب، لكن **لا توجد بوابة دفع** — لا تُخصم أي أموال |
| 8 | **معالج الطلب المخصص** | ✅ **Working** | مساران، 4 خطوات بتحقق، رجوع/تقدّم، ملخص جانبي متجدد، شاشة نجاح برقم طلب |
| 9 | **رفع صورة مرجعية (العميلة)** | ❌ **Not implemented** | زر `Upload` يزيد عدّادًا فقط ويكتب "صورة مرفوعة" — **لا يُفتح مستعرض ملفات ولا تُحفظ صورة** |
| 10 | **رفع صور المنتجات/المعرض (الإدارة)** | ✅ **Working** | `ImgUploader` يفتح الملفات، يضغط الصورة (760px/JPEG)، ويحفظها base64 |
| 11 | **تتبع الطلب برقم** | ✅ **Working** | يبحث في الطلبات ويعرض الحالة وخطًا زمنيًا. جرّب `CBA-1042` |
| 12 | **تغيير حالة الطلب (الإدارة)** | ✅ **Working** | قائمة منسدلة تغيّر الحالة وتضيف سجلًا بالتاريخ — وينعكس فورًا في صفحة التتبع |
| 13 | **طباعة الفاتورة** | ✅ **Working** | نافذة فاتورة كاملة + `window.print()` مع CSS طباعة يخفي الهيدر والفوتر |
| 14 | **CRUD المنتجات/التصنيفات/الكوبونات/المعرض** | ✅ **Working** | إضافة وتعديل وحذف فعلي مع حفظ دائم |
| 15 | **تعديل نصوص الموقع** | ✅ **Working** | تبويب "نصوص الموقع" يعدّل ~60 نصًا مع حفظ تلقائي وانعكاس فوري |
| 16 | **تعديل الإعدادات** | ✅ **Working** | كل الإعدادات تنعكس على الموقع (الشحن، الضريبة، واتساب، السياسات...) |
| 17 | **رفع الشعار من الإعدادات** | ❌ **Not implemented** | **لا يوجد خيار لرفع شعار.** الشعار SVG مكتوب داخل الكود ويحتاج تعديل برمجي |
| 18 | **المفضلة (♡)** | ⚠️ **Partial** | تعمل في صفحة المتجر وتظهر في الحساب، لكنها **معطّلة عمدًا في بطاقات الصفحة الرئيسية**، و**لا تُحفظ** بعد تحديث الصفحة |
| 19 | **تسجيل الدخول** | ⚠️ **Partial** | دخول وهمي بالاسم والجوال بلا تحقق ولا كلمة مرور، **ولا يُحفظ** بعد التحديث |
| 20 | **إضافة/حذف عنوان** | ✅ **Working** | نموذج داخلي يضيف ويحذف العناوين (لكن يضيع مع جلسة الدخول) |
| 21 | **إضافة تقييم منتج** | ✅ **Working** | يُضاف ويُحفظ ويظهر مباشرة تحت المنتج |
| 22 | **نموذج التواصل** | ❌ **Not implemented** | يعرض "تم الإرسال" فقط — **لا يُرسل لأي جهة** |
| 23 | **أزرار واتساب وإنستغرام** | ✅ **Working** | روابط حقيقية تفتح `wa.me` و `instagram.com` بالأرقام من الإعدادات |
| 24 | **الإشعارات** | ⚠️ **Partial** | تُعرَض ويمكن تعليمها مقروءة، لكن **لا تُولَّد تلقائيًا** عند وصول طلب جديد |
| 24ب | **قفل لوحة التحكم** | ⚠️ **Partial** | شاشة دخول تعمل بالكامل (كلمة مرور، رسالة خطأ، جلسة، تسجيل خروج)، لكنها **حماية واجهية غير حقيقية** — كلمة المرور مقروءة من ملف الجافاسكربت |
| 25 | **الأنيميشن** | ✅ **Working** | ظهور تدريجي، حركة عناوين، أزرار مغناطيسية، انتقال بين الصفحات، شريط متحرك — **ويحترم `prefers-reduced-motion`** |
| 26 | **Responsive جوال/كمبيوتر** | ✅ **Working** | مُختبَر على 390px و1280px بلا انزياح أفقي، مع قائمة ☰ على الجوال |
| 27 | **RTL** | ✅ **Working** | `dir="rtl"` كامل مع استخدام صحيح لـ `inset-inline-*` و `margin-inline-*` |
| 28 | **حفظ البيانات** | ⚠️ **Partial** | يُحفظ كل شيء عدا السلة والمفضلة والجلسة — **ومحلي في متصفح واحد فقط** |

### نتيجة الفحص الآلي الأخير
**47/47 اختبار ناجح** (Playwright، على 390px و1280px) — بدون أي خطأ في الـ console، والبناء ينجح بلا تحذيرات.

---

## 9. المشاكل المعروفة

### 🔴 حرجة (تمنع الاستخدام التجاري الحقيقي)
| # | المشكلة | الأثر |
|---|---|---|
| 1 | **لا يوجد Backend** | الطلبات لا تصل لأسماء إطلاقًا. كل طلب يبقى في متصفح العميلة فقط. **الموقع غير جاهز لاستقبال طلبات حقيقية** |
| 2 | **حماية لوحة التحكم واجهية فقط** | يوجد الآن قفل بكلمة مرور (`admin123`)، لكنها مكتوبة في كود الواجهة ويمكن قراءتها من ملف الجافاسكربت. تمنع الزائر العادي ولا تمنع شخصًا تقنيًا — تحتاج تسجيل دخول عبر خادم |
| 3 | **لا توجد بوابة دفع** | اختيار طريقة الدفع شكلي — لا تُخصم أموال |
| 4 | **البيانات محلية لكل متصفح** | ما تعدّله أسماء على جوالها لا يظهر لأي زائر آخر. كل زائر يرى النسخة الافتراضية |

### 🟡 متوسطة
| # | المشكلة | الأثر |
|---|---|---|
| 5 | **لا يوجد Routing حقيقي** | كل الصفحات على `/` — لا يمكن مشاركة رابط منتج، وزر رجوع المتصفح لا يعمل بين الصفحات، ولا فهرسة للصفحات في جوجل |
| 6 | **السلة والمفضلة والجلسة لا تُحفظ** | تحديث الصفحة أثناء التسوق يفرّغ السلة |
| 7 | **رفع الصور المرجعية للعميلة وهمي** | العميلة تظن أنها أرفقت صورة لطلبها المخصص وهي لم ترفع شيئًا — **مضلل للعميلة** |
| 8 | **نموذج التواصل لا يرسل** | يعرض "وصلتنا رسالتك" دون إرسال فعلي — **مضلل للعميلة** |
| 9 | **لا توجد صور للمنتجات** | كل المنتجات والمعرض تعرض بدائل رمادية. **أهم خطوة لجاهزية الموقع: رفع صور حقيقية** |
| 10 | **رقم واتساب افتراضي** | القيمة الحالية `9665XXXXXXXX` — أزرار واتساب لن تعمل حتى يُحدَّث من الإعدادات |
| 11 | **حد تخزين localStorage** | الصور base64 قد تتجاوز ~5MB فتتوقف عملية الحفظ بصمت |
| 12 | **لا يوجد شعار قابل للرفع** | تغيير الشعار يتطلب تعديل الكود |

### 🟢 بسيطة / تقنية
| # | المشكلة | الأثر |
|---|---|---|
| 13 | **ملف واحد بـ 2976 سطرًا** | صعوبة الصيانة والتطوير الجماعي — أهم بند لإعادة الهيكلة |
| 14 | **حزمة JS واحدة 413KB** | لا توجد تجزئة (code splitting) — تحميل أبطأ على شبكات ضعيفة |
| 15 | **الإشعارات لا تُولَّد تلقائيًا** | لوحة التحكم لا تنبّه عند طلب جديد |
| 16 | **المفضلة معطّلة في الصفحة الرئيسية** | عدم اتساق مع صفحة المتجر |
| 17 | **لا توجد اختبارات في المستودع** | الفحص تم بسكربتات خارجية غير محفوظة بالمشروع |
| 18 | **SEO ضعيف** | لا `sitemap.xml`، لا `robots.txt`، لا Open Graph، وصفحة واحدة فقط للفهرسة |

### ✅ ما تم إصلاحه سابقًا (للعلم فقط)
- أرقام الطلبات وُحِّدت إلى `CBA-####` مع ترحيل تلقائي للطلبات المحفوظة
- زر "إضافة عنوان جديد" كان معطّلًا — أُصلح
- انزياح أفقي 10px على الجوال بسبب الشريط المائل — أُصلح
- تكرار "تواصلي معنا" في الفوتر — أُصلح
- بطاقة "القطعة المميزة" كانت ثابتة وغير قابلة للنقر — رُبطت بمنتج حقيقي

---

## 10. ما يحتاج تطوير لاحقًا

### المرحلة 1 — الأساس (ضروري قبل البيع الحقيقي)
1. **Backend حقيقي** — Supabase أو Firebase أو Node/Express
2. **قاعدة بيانات** — للمنتجات والطلبات والعملاء (بدل `localStorage`)
3. **حماية حقيقية للوحة التحكم** — تسجيل دخول عبر خادم بدل كلمة المرور الحالية المكتوبة في الواجهة
4. **إشعار الطلبات الجديدة** — بريد أو واتساب أو Telegram لأسماء عند كل طلب
5. **رفع صور المنتجات الحقيقية** — أهم خطوة بصرية
6. **تحديث رقم واتساب وحساب إنستغرام** من الإعدادات

### المرحلة 2 — التجارة
7. **بوابة دفع سعودية** — Moyasar أو Tap أو PayTabs (مدى، Apple Pay، بطاقات)
8. **شركات الشحن** — SMSA أو Aramex مع تتبع حقيقي
9. **تسجيل دخول العميلات** — OTP عبر الجوال
10. **إدارة حقيقية للمخزون** — خصم تلقائي عند الشراء
11. **حفظ السلة** — للزائرة عبر الجلسات وبين الأجهزة
12. **رفع الصور المرجعية فعليًا** في الطلب المخصص

### المرحلة 3 — الجودة والنمو
13. **إعادة هيكلة الكود** — تقسيم `App.jsx` إلى `components/`, `pages/`, `data/`, `styles/`
14. **إضافة Router** — `react-router` لمسارات حقيقية (`/shop`, `/product/p1`)
15. **تحسين SEO** — عناوين ووصف لكل صفحة، Open Graph، sitemap، بيانات منظمة للمنتجات
16. **تحسين الأداء** — code splitting، تحميل كسول للصور، تقليل حجم الحزمة
17. **تحسين الأمان** — تحقق من المدخلات، حماية من XSS، صلاحيات
18. **ربط واتساب للطلبات** — إرسال ملخص الطلب تلقائيًا
19. **اختبارات آلية** — إضافة Playwright للمستودع مع CI
20. **تحليلات** — Vercel Analytics أو Google Analytics
21. **نموذج تواصل فعلي** — عبر خدمة بريد
22. **نقاط الولاء** — الميزة موعودة في صفحة الحساب

---

## 11. ملفات مهمة للمراجعة

| الأولوية | الملف | لماذا |
|---|---|---|
| 1️⃣ | `package.json` | التبعيات والأوامر — دقيقتان |
| 2️⃣ | `vite.config.js` · `vercel.json` · `index.html` | إعدادات البناء والنشر — 5 دقائق |
| 3️⃣ | **`src/App.jsx`** | **99% من المشروع** — راجع القسم 12 للترتيب الداخلي |
| 4️⃣ | `src/main.jsx` · `src/index.css` | صغيران جدًا (10 و3 أسطر) |
| 5️⃣ | `README.md` | تعليمات التشغيل المختصرة |
| — | `public/favicon.svg` | الأصل الخارجي الوحيد |

> ⚠️ **لا توجد ملفات routes أو components أو data منفصلة** — كلها مدمجة في `src/App.jsx`.

---

## 12. تعليمات للمساعد التقني

### إذا أردت مراجعة المشروع، ابدأ بهذا الترتيب:

**الخطوة 0 — شغّل المشروع أولًا (10 دقائق)**
```bash
git clone https://github.com/crochetbyasma/crochetbyasma.git
cd crochetbyasma
npm install
npm run dev
```
تجوّل في الموقع كعميلة: تصفّح المتجر → افتح منتجًا → أضِفه للسلة → طبّق `SHUKRAN10` → أكمل الطلب → تتبّع الطلب. ثم افتح "لوحة التحكم" وجرّب التبويبات. **هذا يوفّر عليك ساعة من قراءة الكود.**

**الخطوة 1 — الإعدادات العامة (5 دقائق)**
1. `package.json` — تأكد أن التبعيات هي React فقط
2. `vite.config.js` و `vercel.json` — إعداد البناء والنشر
3. `index.html` — الـ meta و `dir="rtl"`

**الخطوة 2 — افهم البيانات قبل الواجهة (15 دقيقة)**
داخل `src/App.jsx`:
4. السطر ~426–612 — كل ثوابت `initial*` (المنتجات، الطلبات، الإعدادات، النصوص)
5. السطر ~945 — `DB_KEY` و `dbLoad`/`dbSave`/`dbClear` — طبقة التخزين كاملة
6. السطر ~612–660 — الأدوات المساعدة: `SAR`, `calcTotals`, `statusLabel`

**الخطوة 3 — المكوّن الجذر (20 دقيقة)**
7. السطر ~2740 — `function App`: كل الحالة، والتحميل والحفظ، ودوال `addToCart` و `createOrder` و `toggleFav`، والهيدر والفوتر والتوجيه
> هذا أهم مقطع في المشروع كله — إذا فهمته فهمت كل شيء.

**الخطوة 4 — رحلة الشراء (30 دقيقة)**
8. `ProductCard` (~1005) → `Shop` (~1260) → `ProductDetail` (~1328) → `CartPage` (~1806) → `Track` (~1954)

**الخطوة 5 — الطلب المخصص (15 دقيقة)**
9. `Wizard` (~1587) — أهم ميزة تجارية في الموقع

**الخطوة 6 — لوحة التحكم (30 دقيقة)**
10. `Admin` (~2100) ثم تبويباتها: `AdminOrders`, `AdminProducts`, `AdminTexts`, `AdminSettings`, `InvoiceModal`

**الخطوة 7 — التنسيق (اختياري)**
11. السطر ~14–425 — متغير `CSS`: متغيرات الألوان في `:root` ثم الأصناف

### نقاط انتبه لها أثناء المراجعة
- **لا تبحث عن ملفات مكونات منفصلة** — كل شيء في `App.jsx`
- **لا تبحث عن API أو fetch** — لا يوجد أي اتصال شبكي إطلاقًا
- **الهوية البصرية مقصودة** — الألوان (تركواز `#2F6664`، بيج `#F8F1E8`، خوخي `#F6C7AD`، أوف وايت) والرسومات اليدوية جزء من العلامة، والمالكة طلبت الحفاظ عليها
- **`Upload` ≠ `ImgUploader`** — الأول وهمي للعميلة، الثاني حقيقي للإدارة
- **النصوص قابلة للتعديل من لوحة التحكم** — لا تغيّر النصوص في الكود مباشرة إلا للصياغات الافتراضية، وأضِف ترحيلًا في `TEXT_MIGRATIONS` عند تغيير نص افتراضي

### أولوياتي المقترحة إذا سُئلت "من أين نبدأ؟"
1. **Backend + قاعدة بيانات** (Supabase أسرع طريق) — يحل مشاكل 1 و4 و6 دفعة واحدة، ويأتي معه تسجيل دخول حقيقي يستبدل القفل الواجهي الحالي
2. **إشعار الطلبات لأسماء** — بدونه الموقع لا يعمل تجاريًا
3. **رفع صور المنتجات** — أكبر أثر بصري بأقل جهد
4. **بوابة دفع سعودية**
5. **إعادة الهيكلة + Router + SEO**

---

## ملحق: معلومات النشر

| البند | القيمة |
|---|---|
| **المنصة** | Vercel |
| **Project ID** | `prj_ISxsSkirPkt3oC0uCJ7M5LKCbaZO` |
| **النشر التلقائي** | عند كل دمج في `main` |
| **مدة البناء** | ~5–10 ثوانٍ |
| **الدومينات** | `crochetbyasma.com` · `www.crochetbyasma.com` · `crochetbyasma-crochetbyasma-team.vercel.app` |
| **حالة آخر نشر** | ✅ READY — بدون أخطاء |
